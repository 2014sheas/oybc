# Non-secret Functions params for project "demo-oybc-functions-test" (loaded by firebase deploy and
# the emulator; see functions/README.md). Never put secrets here — secrets use
# defineSecret / Secret Manager (.env*.local is gitignored for local overrides).
#
# Dev/test accepts RevenueCat SANDBOX purchases. The PROD project's file
# (.env.<prod-project-id>) MUST set this to PRODUCTION only.
REVENUECAT_ALLOWED_ENVIRONMENTS=PRODUCTION,SANDBOX
