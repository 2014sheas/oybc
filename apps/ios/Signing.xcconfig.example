// Per-developer signing settings.
//
// Copy this file to `Signing.xcconfig` and set DEVELOPMENT_TEAM to your
// Apple Developer Team ID (found in Xcode → Settings → Accounts, or in
// the Apple Developer portal).
//
// `Signing.xcconfig` is gitignored so each contributor can set their own
// team ID without affecting others / CI.

DEVELOPMENT_TEAM =
