{
  "_comment": "Drift guardrail baseline for knip (see scripts/check-knip.mjs). Each entry is a known unused web export/type frozen as accepted debt: the guardrail fails only on NEW unused exports not listed here. Shrink this list as dead code is deleted (the guardrail notes when a baselined entry is gone). New unused FILES/DEPENDENCIES/unlisted-imports fail immediately and are never baselined.",
  "unusedExports": []
}
