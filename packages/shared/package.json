{
  "name": "@oybc/shared",
  "version": "2.0.0",
  "description": "Shared types, algorithms, and validation for OYBC",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "clean": "rm -rf dist",
    "lint": "tsc --noEmit",
    "gen:sync-contract": "node scripts/generate-sync-contract.js",
    "gen:sync-fixtures": "node scripts/sync-fixtures-to-ios.js"
  },
  "keywords": [
    "bingo",
    "shared",
    "types"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@oybc/bingo-core": "workspace:*",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^26.1.0",
    "jest": "^30.4.2",
    "ts-jest": "^29.4.11",
    "typescript": "^6.0.3"
  }
}
